{
  "name": "dnd-ai-sim",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/shared",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/web",
    "test": "npm run test --workspace=packages/shared",
    "seed": "npm run seed --workspace=packages/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
